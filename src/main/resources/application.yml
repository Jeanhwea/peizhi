server:
  port: 8080

# spring boot
spring:
  application:
    name: peizhi
  cloud:
    config:
      server:
        git:
          uri: http://192.168.0.202/avic/configfiles.git
          searchPaths:
            - common
            - database
            - services
          username: jiqiren
          password: pa$$word

# logging
logging:
  file: log/peizhi.log
  group:
    app: com.avic.mti.peizhi
  pattern:
    console: "%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){cyan} %clr(%5p): %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} %5p: %msg%n"
  level:
    root: INFO
    app: TRACE
    org.springframework.web: DEBUG
    org.hibernate: ERROR